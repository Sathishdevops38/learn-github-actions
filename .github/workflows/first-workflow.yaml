name: first workflow
on:
  push:
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
  second-job:
    needs: first-job
    runs-on: ubuntu-latest
    steps:
      - run: |
          date
          pwd    

  